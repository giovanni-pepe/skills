interface:
  display_name: "Angular OIDC Guard Hardening"
  short_description: "Harden Angular OIDC guards and token flow"
  default_prompt: "Use $angular-oidc-guard-hardening to audit and harden Angular OIDC guards, interceptors, and refresh behavior."

policy:
  allow_implicit_invocation: true
