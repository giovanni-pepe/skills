interface:
  display_name: "Django + Angular Nx Delivery Loop"
  short_description: "Deliver combined Django + Angular Nx changes safely"
  default_prompt: "Use $django-angular-nx-delivery-loop to implement coordinated Django and Angular Nx changes with repo-native verification."

policy:
  allow_implicit_invocation: true
