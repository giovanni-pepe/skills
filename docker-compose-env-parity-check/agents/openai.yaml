interface:
  display_name: "Docker Compose Env Parity Check"
  short_description: "Detect compose env and service drift safely"
  default_prompt: "Use $docker-compose-env-parity-check to compare compose environments and classify safe alignment steps."

policy:
  allow_implicit_invocation: true
