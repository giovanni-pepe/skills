interface:
  display_name: "Django + Angular Keycloak OAuth2 Config Audit"
  short_description: "Audit combined Django + Angular auth config parity"
  default_prompt: "Use $django-angular-keycloak-oauth2-config-audit to audit Keycloak and OAuth2 config parity across the Django and Angular layers."

policy:
  allow_implicit_invocation: true
