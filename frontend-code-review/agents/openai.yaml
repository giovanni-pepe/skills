interface:
  display_name: "Review Codice Frontend"
  short_description: "Review frontend con findings per severita"
  default_prompt: "Usa $frontend-code-review per fare review pre-merge del codice frontend cambiato e riportare solo problemi critical, major e minor."

policy:
  allow_implicit_invocation: true